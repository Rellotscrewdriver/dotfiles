(deflisten bluebar `./bar/scripts/bluetooth.sh`)

(defwidget bluebar [] 
  (eventbox :cursor "pointer" :onclick "exec foot bluetui &" :onrightclick "./bar/popup-menus/scripts/toggle-bt.sh" (label :text "${bluebar.icon}" :class "bt-i"))
)

(deflisten cava :initial "▁▁▁▁▁▁" `./bar/scripts/cava`)

(defwidget cava []
	(eventbox :onclick "exec swayosd-client --playerctl play-pause" :tooltip "Song: ${musicSong}"
    (label :text "${cava}" :class "cava")
  )
)

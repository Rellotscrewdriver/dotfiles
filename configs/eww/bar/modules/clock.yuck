(defpoll dt :interval "5s" "date +\"%-I:%M %p\"")

(defwidget clock [] 
	(box :space-evenly false :orientation "h"
  (eventbox :cursor "pointer" :onclick "exec gnome-clocks"(label :text "󰥔" :class "clk-i"))
	(eventbox :cursor "pointer" :onclick "./bar/popup-menus/scripts/toggle-cal.sh" (label :text "${dt}" :class "clk-t")))
)

(deflisten bluebar `./bar/scripts/bluetooth.sh`)

(defwidget bluebar [] 
  (eventbox :cursor "pointer" :onclick "exec foot bluetui &" :tooltip "Connected to: ${bluetooth.name}" (label :text "${bluebar.icon}" :class "bt-i"))
)

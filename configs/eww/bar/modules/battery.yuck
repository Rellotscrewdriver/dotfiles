(deflisten baticon `./bar/scripts/baticon.sh`)

(defwidget battery [] 
	(box :space-evenly false :orientation "h" :class "bat-t"
  (circular-progress :class "bat" :value {EWW_BATTERY.BAT0.capacity} :thickness 3 :start-at 25
    (box :class "batico"
      (label :text "${baticon}")
    )
  )

  )
)

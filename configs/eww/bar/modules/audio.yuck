(deflisten audio `./bar/scripts/audio`)

(defwidget audio [] 
	(box :space-evenly false :orientation "h"
    (eventbox :cursor "pointer" (label :text "${audio.icon}" :class "audI")
	    (label :text "${audio.value}" :class "audT")    
    )
  )
)

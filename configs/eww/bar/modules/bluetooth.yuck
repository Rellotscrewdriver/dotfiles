(deflisten bluethbar `./bar/scripts/bluetooth`)

(defwidget bluethbar [] 
  (eventbox :cursor "pointer" :onclick "exec foot bluetui &" :tooltip "Connected to: ${bluetooth.name}" 
    (label :text "${bluethbar.icon}" :class "btI")
  )
)
